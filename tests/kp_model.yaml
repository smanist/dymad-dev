data:
  path: './data/kp.npz'
  n_samples: 64
  n_steps: 31
  double_precision: true

transform_x:
  type: "scaler"
  mode: "std"

transform_u:
  type: "identity"

split:
  train_frac: 0.75

dataloader:
  batch_size: 64